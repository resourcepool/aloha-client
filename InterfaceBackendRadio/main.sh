#!/bin/bash
curl -X POST -d uid="UID" -d "firstName"="firstName" -d "lastName"="lastName" -d "status"="status" -d "description"="description"  -d "firstName"="FIRSTNAME" -d "tags"="tags"  localhost:5000/message/UIiiiDauie
